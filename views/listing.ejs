<div class="box bike ${ listing.bike_type }">
  <div class="title">
    <h2><%= listing.title %></h2>
  </div>
  <div class="body">
    <div class="description">
      <p><%= listing.description %></p>
      <div class="price">
        <span><%= listing.price %></span>
        per day
      </div>
    </div>
    <div class="info">
      <div>
        <strong>Location</strong>
        <span>Palo Alto, CA</span>
      </div>
      <div>
        <strong>Type</strong>
        <span><%= listing.bike_type %></span>
      </div>
      <div>
        <% if(!hide_button) { %>
        <a href="/rent/<%= listing.id %>" class="btn btn-inverse">Rent this bike</a>
        <% } %>
      </div>
    </div>
  </div>
</div
